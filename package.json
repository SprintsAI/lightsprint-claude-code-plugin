{
  "name": "lightsprint-claude-code-plugin",
  "version": "0.5.13",
  "description": "Sync Claude Code tasks to Lightsprint repo board",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bash scripts/compile.sh",
    "test": "bun test",
    "deploy:tag": "bash scripts/deploy-tag.sh"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
