{
  "name": "lightsprint",
  "version": "0.6.4",
  "description": "Manage Lightsprint tasks and hand work to managed cloud agents",
  "type": "module",
  "bin": {
    "lightsprint": "npx-install.js"
  },
  "files": [
    "npx-install.js"
  ],
  "scripts": {
    "build": "bash scripts/compile.sh",
    "test": "bun test",
    "deploy:tag": "bash scripts/deploy-tag.sh"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@sentry/node": "^10.45.0"
  }
}
