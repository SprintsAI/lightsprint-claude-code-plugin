{
  "name": "lightsprint",
  "version": "0.5.13",
  "description": "Sync Claude Code tasks to Lightsprint repo board",
  "type": "module",
  "bin": {
    "lightsprint": "scripts/lightsprint.js"
  },
  "files": [
    "scripts/**/*.js",
    "!scripts/__tests__/",
    "skills/",
    "hooks/",
    ".claude-plugin/"
  ],
  "scripts": {
    "build": "bash scripts/compile.sh",
    "test": "bun test",
    "deploy:tag": "bash scripts/deploy-tag.sh"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
