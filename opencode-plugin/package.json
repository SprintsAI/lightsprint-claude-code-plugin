{
  "name": "@lightsprint/opencode",
  "version": "0.4.3",
  "description": "Lightsprint plugin for OpenCode - plan review and task management",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build index.ts --outdir dist --target bun",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.1.10"
  },
  "peerDependencies": {
    "bun": ">=1.0.0"
  }
}
